# ui_hud_v1 Prompt

Use case: game asset UI sprite sheet

Asset type: production UI atlas for a Godot 4 2D web game HUD

Primary request: Create one clean sci-fi tactical HUD UI sprite sheet on a perfectly flat solid `#ff00ff` chroma-key background. Arrange eight isolated assets in a strict four-column by two-row grid, each asset centered inside its cell with generous padding, no visible grid lines, no labels, no text, no numbers, no watermark.

Assets in reading order:

1. Draw pile icon: a stacked deck of pristine tactical cards with cyan edge lights.
2. Discard pile icon: a messy stack of worn cards with one corner burned or greyed, amber warning accent.
3. Discard hand button icon: three cards flying into a small bin or void arrow, clear discard action silhouette.
4. Pause button icon: two vertical pause bars inside a compact metal hexagonal button plate.
5. Health bar UI: long horizontal health meter frame, red inner fill strip, fortified metal frame, suitable for a ProgressBar texture.
6. Experience bar UI: long horizontal experience meter frame, blue-cyan inner fill strip, slim polished frame.
7. Energy value UI icon: circular or hex energy core badge, magenta-violet core with cyan rim, readable at 48 px.
8. Energy bar UI: segmented long horizontal energy meter, magenta-violet fill, cyan/steel frame, suitable for a ProgressBar texture.

Style: polished 2D game UI, post-apocalyptic sci-fi city defense, dark brushed metal, worn edges, subtle amber hazard details, cyan energy accents, matching the existing card frame, defense wall, and battlefield art style.

Constraints: perfectly flat uniform `#ff00ff` background only; do not use `#ff00ff` in the assets; no floor plane, no gradient background, no cast shadow, no contact shadow, no text, no labels, no characters.
