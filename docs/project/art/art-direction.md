# 美术方向

## 当前锁定风格

- 未锁定

## 风格候选

- 选中风格候选图：未选择
- 素材落地状态：未开始

## 素材计划

- 待创建新游戏后写入。

## 运行时素材要求

- 风格候选图不等于运行时素材。
- HUD、角色、威胁/目标、场景和 VFX 必须服从同一视觉规范。
- 运行时素材必须放入 `assets/` 或 `addons/`。
- `references/` 只能放参考资料，不允许被运行时代码直接加载。
