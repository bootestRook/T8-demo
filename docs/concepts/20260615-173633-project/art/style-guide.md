# 风格指南

当前模板未锁定美术风格。新游戏 init 选定风格候选后，本文件必须记录同源视觉资产规则。

## 色板

- 待创建新游戏后写入。

## 线条与材质

- 待创建新游戏后写入。

## 角色比例

- 待创建新游戏后写入。

## 场景规则

- 待创建新游戏后写入。

## UI 形状语言

- 待创建新游戏后写入。

## VFX 气质

- 待创建新游戏后写入。

## 运行时素材包

- 玩家、威胁/目标、场景、HUD/UI 和核心 VFX 必须来自同一风格规范。
- 运行时素材进入 `assets/sprites/`、`assets/ui/`、`assets/generated/runtime/` 或 `addons/`。

## 禁止事项

- 不得把概念图裁剪块、`style_candidates/` 候选图、`references/` 参考图或临时 workspace 文件直接作为运行时素材。
